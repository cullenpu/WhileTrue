{
    "scripts": {
      "install-all": "npm i --prefix server && npm i --prefix app",
      "build": "npm run install-all && npm run build --prefix app",
      "start": "npm run start:prod --prefix server"
    },
    "devDependencies": {
      "concurrently": "^5.3.0",
      "install": "^0.13.0",
      "npm": "^6.14.8",
      "prettier": "^2.1.2"
    }
}